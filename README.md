> IMPORTANT: Use Ruby2.7

install

```bash
gem install jekyll bundler
```

prepare
```bash
/bin/bash --login`
source ~/.bashrc
rvm use 2.7
```

run locally
```bash
bundle install
bundle exec jekyll serve
```